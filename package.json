{
  "name": "fastify-access-logger",
  "version": "0.2.0",
  "description": "an access logger plugin for fastify",
  "main": "lib/AccessLoggerPlugin.js",
  "scripts": {
    "unit": "tap test/test.js",
    "test": "npm run lint && npm run unit",
    "lint": "standard lib/* test/*",
    "coveralls": "npm run unit --  --cov",
    "coverage": "npm run coveralls && tap --coverage-report=lcov"
  },
  "author": "Denis Fäcke",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.0.2",
    "fastify": "^2.0.0-rc.3",
    "request": "^2.88.0",
    "standard": "^12.0.1",
    "tap": "^12.1.1"
  },
  "dependencies": {
    "each-async": "^1.1.1",
    "fast-date-format": "^2.0.0",
    "fastify-plugin": "^1.4.0",
    "generate-function": "^2.3.1"
  },
  "engines": {
    "node": ">= 6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SerayaEryn/fastify-access-logger.git"
  },
  "keywords": [
    "access logger",
    "access",
    "logger",
    "fastify",
    "plugin"
  ]
}
